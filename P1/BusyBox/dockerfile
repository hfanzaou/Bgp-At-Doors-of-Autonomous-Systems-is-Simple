# Use Alpine as the base image
FROM alpine:latest

# Install BusyBox or equivalent (BusyBox comes pre-installed in Alpine)
RUN apk update && apk add busybox

# Set the default command to run BusyBox
CMD ["busybox"]

